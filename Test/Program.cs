using ServerModel.GameMechanics;
using ILibrary.Maths.Geometry2D;
using GameCore.Tools;

namespace Test
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            //Bacterium start = new Bacterium(1, UnityEngine.Vector2.one, 1, 1);
            //Bacterium end = new Bacterium(1, UnityEngine.Vector2.one, 1, 1);
            //Road road = new Road();

            //VirusGroup virusGroup = new VirusGroup(start, end, 100, road, 2f);
            //byte[] buffer = VirusGroup.BitConverter.GetBytes(virusGroup);
            //virusGroup = VirusGroup.BitConverter.GetInstance(buffer);


            //GameSettings gameSettings = new GameSettings(new BacteriumData[1] { new BacteriumData(GameCore.Enums.OwnerType.Enemy, new Transform(1, 1, new Circle(new UnityEngine.Vector2(1, 1), 1)), 3) });
            //byte[] buffer = GameSettings.BitConverter.GetBytes(gameSettings);
            //gameSettings = GameSettings.BitConverter.GetInstance(buffer);
        }
    }
}